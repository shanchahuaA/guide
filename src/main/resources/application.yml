spring:
  application:
    name: guide
  profiles:
    include: local
  devtools:
    restart:
      enabled: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/peak_guide?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    # 账号和密码在 application-local.yml 里覆盖
    username: ""
    password: ""

server:
  port: 8080
