# 领域词汇表

> 这份文件只放**词汇定义**，不放实现细节、不放待办、不放设计理由。
> 新人读代码前先读这一页，避免把不同的东西叫成同一个名字。

---

## 图鉴条目 (Item)

图鉴里的一条记录，**一条 = 游戏里的一个物品**。

- 判断是不是同一条的判据是：**它在游戏里是不是一个独立物品**，不是"熟没熟"。
- 所以 `Hot Dog` 生吃熟吃是**同一条**（烹饪只是它的一个状态）；`Cooked Bird` 是**独立的一条**，因为它在游戏里只能从 `Big Egg` 开出、开出来就已经是熟的。
- 首期共 **134 条**：食物与道具两个模块的合计（早期估算口径是 130，实测数据源全量后为 134）。食物与道具**完全互斥**，没有一条同时属于两边 —— 归属由**主类型**计算得出（见下文「主类型」），一个条目只进一个模块。
- 134 条里含 **2 条游戏内已移除**（`Bugle?`、`Warp Compass`）和 **3 个毒蘑菇变体**（如 `Bugle Shroom (Poisonous)`，各自独立成条）。**列表不过滤已移除条目** —— 图鉴收录游戏里看不到的东西，这是**明确的产品决定**。
- 条目数是图鉴的公信力来源：**游戏里出现过多少个物品，图鉴就有多少条**，不做人为拆分。

## 生 / 熟 (Raw / Cooked)

同一个物品的**两种状态**，不是两个物品。

- 用户游戏里的行为是"把食物拿到火上烤"，烤完手里还是同一个东西。所以详情页要能**一屏看到生熟对比**。
- 采用这个模型，用户搜 "Hot Dog" 只会搜到**一条**记录，点进去就能对比两种吃法。
- 反例警告：如果把生熟拆成两条，用户搜 "Hot Dog" 会看到两条互不相干的记录，而**对比生熟正是这个页面最主要的用途**。

## 可烹饪 (Cookable)

一个**判据字段**，不是"这个物品有没有饱食度"。

- 置为真的条件：**存在非零熟食数值** —— 该条目的熟食数值（饱食、加成等）里至少有一个不为 0。仅**食物**参与判定。
- 反例：`First Aid Kit`（写着 `No effect.`）、`Dynamite`（写着会爆炸）、`Fortified Milk` 都不是可烹饪的，详情页要**隐藏熟食区块**，而不是显示一堆 0。它们的烹饪说明文字进 `description`，信息不丢。
- 为什么单独拎出来：如果无脑给每个食物都生成熟食，会造出一堆**没有意义的空条目**，拉低图鉴观感。

## 主类型 (Primary Type)

图鉴**一级导航**用的类别。

- 取值：`FOOD` / `CONSUMABLE` / `EQUIPMENT` / `DEPLOYABLE` / `AMULET` / `MYSTICAL` / `MISC` / `ENEMY`。
- **归约规则**（主类型是计算值，不落库）：类型中含 `Food` 即归 `FOOD`；否则按 **`CONSUMABLE`（消耗品）> `EQUIPMENT`（装备）> `DEPLOYABLE`（可放置）> `AMULET`（护身符）> `MYSTICAL`（神秘）> `MISC`（杂物）** 的顺序取第一个命中的类型；纯敌人（只有 `Enemy`）归 `ENEMY`。
- 一级导航归属同样计算得出，不存储：主类型为 `FOOD` 的进**食物**模块，其余进**道具**模块。
- 一个物品在数据源里可能有**多个**分类标签（比如 `Food, Natural food, Berry`），但一级导航必须收敛成**一个**入口，所以主类型是**有意冗余**的，不是设计失误。

## 标签 (Tag)

数据源里的多值分类，用来给图鉴做**二级筛选**。

- 例：`Berry`（浆果）、`Mushroom`（蘑菇）、`Natural food`、`Packaged food`。
- 标签按**维度**归类：`type`（分类，即上面的例子）、`biome`（生态）、`source`（来源）、`rarity`（稀有度）、`location`（位置）、`flag`（标记，如 `cookable` 可烹饪、`removed` 已移除）。
- 与主类型的区别：主类型决定"从哪个门进"，标签决定"进去之后怎么筛"。
- **标签是交叉的**：一条记录可以同时是 `Food` + `Natural food` + `Berry`。所以图鉴里"一件东西出现在多个栏目"是**正常现象**，不是数据重复。
- 与主类型的另一个区别：主类型有且只有一个，标签有 0 到多个。

## 生态 (Biome)

物品会出现的游戏地区，用来做**按地区筛选**。

- 例：`Tropics`、`Roots`、`Mesa`、`Shore`、`Gloom`。
- 与"来源"的区别：生态是**在哪捡到**，来源是**怎么得到的**。
- **数据存于条目的标签结构中**（`tag.code = "biome"`），不单独占列。

## 来源 (Source)

物品的**获取方式**，只用于展示，不用于筛选。

- 例：`Regular Luggage`、`Big Luggage`、雕像。
- 为什么不做筛选：用户不会按"来源容器"逛图鉴，这里只做说明性展示。
- **数据存于条目的标签结构中**（`tag.code = "source"`），不单独占列。

## 稀有度 (Rarity)

物品的**稀有程度**，只用于展示。

- 例：`Common`、`Uncommon`、`Rare`、`Epic`、`Mythic`、`Legendary`。
- **数据存于条目的标签结构中**（`tag.code = "rarity"`），不单独占列。

## 状态效果 (Status Effect)

物品对玩家的影响，一个有正负号的数值。

- 例：`hunger`（饱食）、`bonus`（加成）、`heat`/`cold`（冷热）、`injury`（伤害）、`poison`（中毒）、`spores`（孢子）、`drowsy`（困倦）、`curse`（诅咒）、`thorns`（荆棘）。
- **正负号有语义**：负值表示**消除/减少**该状态。
- 状态是**封闭集合**（实测约 14 种，含带持续时间的变体），所以新增状态是罕见事件，不是日常操作。
- 每个状态可以带**持续时间**和**延迟生效**两个附属值。

## 图鉴 (Compendium)

这个产品的**核心功能**：一个纯查询工具。

- **不记录收集进度**。游戏内部本来就没有图鉴，用户随时来查数据，查完就走。
- 这个定位是一个**已经确认的产品决定**，它排除了"收集率""我的背包"这类功能。
- 它不是"另一个 PEAK Wiki"：数据是结构化的、有中文、有生熟对比，而不是照搬英文页面的散文。

## 数据源 (Data Source)

`peak.wiki.gg` —— PEAK 游戏的社区 Wiki。

- 底层是 MediaWiki + **Cargo** 扩展，核心数据在一张叫 `Items` 的结构化表里，可以用 `cargoquery` 当只读 SQL 接口查。
- **不要爬 HTML**：HTML 页面有 Cloudflare JS 挑战，而且散文里没有结构化字段。
- 数据源用的是 **CC BY-SA 4.0** 许可证，要求**署名**且**相同方式共享**。图鉴里需要有一个"数据来源"说明。

## 爬虫采集 (Crawler)

**管理员后台的一个功能**，不是小程序的运行时功能。

- 职责：从**数据源**拉全量结构化数据 → 转换成**图鉴条目**的字段结构 → 落库。
- 它是一次性的、离线跑的导入工具。**用户永远不触发它**。
- 它最脏的部分不是"下载"，而是"**转换**"：多值字段要拆开、熟食数值要按公式和 wikitext 覆盖值算出来、要按**可烹饪**判据过滤掉无意义的条目。

## AI 助手 (AI Assistant)

小程序里的对话式功能，回答用户关于游戏的问题。

- 边界：**图鉴内的问题**（"蘑菇有什么用""这个道具怎么获得"）由后端查**图鉴数据**回答；**路线类问题**（"今日最佳路线"）走**联网搜索**，在 B站 搜当天日期的攻略并给出链接。
- 关键区分：**图鉴内问题不联网**，因为答案是你们自己的数据，联网只会引入错误。只有路线类问题联网。
- 用户需要**自己提供大模型 API Key** 才能使用 —— 这是有意的设计，让服务端不承担调用成本。

## 演示环境 (Demo Environment)

用来演示的那台电脑上的运行时状态，**不是生产环境**。

- 后端跑在 `localhost:8080`，小程序在**开发者工具**里跑，并勾选"不校验合法域名"。
- 这是**刻意选择**的：真机演示需要一个**已备案的 HTTPS 域名**，备案周期 2-3 周，与本项目的时间约束冲突。
- 含义：本项目的"能跑"是指**在演示环境下能跑**。部署到公网是**范围外**的事，不要为此写代码。
